# SECURITY: Never commit actual secrets to git
# All files in this directory are ignored except documentation

# Ignore all secrets
*

# But keep the directory structure and documentation
!.gitignore
!README.md
!template/
!template/*

# Ignore encrypted secrets (these should be managed separately)
*.enc
*.gpg
*.age
