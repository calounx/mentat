# Python requirements for observability-stack YAML tools
# Install with: pip install -r requirements.txt

# Core dependencies
PyYAML>=6.0

# Schema validation (required for validate_schema.py)
jsonschema>=4.0.0

# HTTP requests (required for validate-exporters.py)
requests>=2.31.0
